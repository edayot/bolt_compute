

data_pack:
  load: .

require:
  - beet.contrib.snapshot
  # - mecha.contrib.debug_ast
  - bolt
  - bolt_compute

pipeline:
  - mecha

minecraft: "26.3-snapshot-10"

output: build

meta:
  debug_ast:
    location: true
